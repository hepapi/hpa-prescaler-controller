spec:
  template:
    spec:
      containers:
        - name: argocd-repo-server
          volumeMounts:
            - name: demo-charts
              mountPath: /demo-charts
              readOnly: true
      volumes:
        - name: demo-charts
          hostPath:
            path: /demo-charts
            type: Directory
